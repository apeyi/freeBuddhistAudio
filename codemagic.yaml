workflows:
  ios-build:
    name: iOS Build & Test
    max_build_duration: 30
    instance_type: mac_mini_m2
    environment:
      xcode: latest
    triggering:
      events:
        - push
        - pull_request
      branch_patterns:
        - pattern: '*'
    scripts:
      - name: Install XcodeGen
        script: brew install xcodegen

      - name: Generate Xcode project
        script: cd FBAudio-iOS && xcodegen generate

      - name: Build
        script: |
          set -o pipefail
          cd FBAudio-iOS
          xcodebuild build \
            -project FBAudio.xcodeproj \
            -scheme FBAudio \
            -destination 'generic/platform=iOS Simulator' \
            -skipPackagePluginValidation \
            CODE_SIGNING_ALLOWED=NO 2>&1 | tee ../build.log

      - name: Export simulator app
        script: |
          APP_PATH=$(find ~/Library/Developer/Xcode/DerivedData -name "FBAudio.app" -path "*/Debug-iphonesimulator/*" | head -1)
          if [ -n "$APP_PATH" ]; then
            cd "$(dirname "$APP_PATH")"
            zip -r ~/FBAudio-simulator.zip FBAudio.app
          fi

      - name: Test
        script: |
          set -o pipefail
          cd FBAudio-iOS
          SIM_ID=$(xcrun simctl list devices available -j | python3 -c "
          import json, sys
          data = json.load(sys.stdin)
          for runtime, devices in data.get('devices', {}).items():
              if 'iOS' in runtime:
                  for d in devices:
                      if 'iPhone' in d.get('name', '') and d.get('isAvailable'):
                          print(d['udid']); sys.exit(0)
          ")
          xcodebuild test \
            -project FBAudio.xcodeproj \
            -scheme FBAudio \
            -destination "platform=iOS Simulator,id=$SIM_ID" \
            -skipPackagePluginValidation \
            CODE_SIGNING_ALLOWED=NO 2>&1 | tee ../test.log

    artifacts:
      - build.log
      - test.log
      - ~/FBAudio-simulator.zip
