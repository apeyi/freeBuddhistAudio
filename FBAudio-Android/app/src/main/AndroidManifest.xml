<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".FBAApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="Free Buddhist Audio"
        android:supportsRtl="true"
        android:theme="@style/Theme.FBAudio"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.FBAudio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="www.freebuddhistaudio.com"
                    android:pathPrefix="/audio/details" />
                <data
                    android:scheme="https"
                    android:host="www.freebuddhistaudio.com"
                    android:pathPrefix="/series/details" />
                <data
                    android:scheme="https"
                    android:host="www.freebuddhistaudio.com"
                    android:pathPrefix="/browse" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="freebuddhistaudio.com"
                    android:pathPrefix="/audio/details" />
                <data
                    android:scheme="https"
                    android:host="freebuddhistaudio.com"
                    android:pathPrefix="/series/details" />
                <data
                    android:scheme="https"
                    android:host="freebuddhistaudio.com"
                    android:pathPrefix="/browse" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="fbaudio" android:host="talk" />
                <data android:scheme="fbaudio" android:host="series" />
                <data android:scheme="fbaudio" android:host="speaker" />
            </intent-filter>
        </activity>

        <!-- Media session + browse tree. The legacy MediaBrowserService action is
             what Android Auto (and Bluetooth/Assistant) connect through; Media3's
             MediaLibraryService serves both. -->
        <service
            android:name=".player.PlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaLibraryService" />
                <action android:name="androidx.media3.session.MediaSessionService" />
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- Lets the system restart playback from the media notification / a
             Bluetooth play button after the process has been killed
             (MediaSession.Callback.onPlaybackResumption). -->
        <receiver
            android:name="androidx.media3.session.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <!-- Android Auto: this is a media app -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <!-- WorkManager's foreground service declares NO foregroundServiceType in
             its own manifest; on Android 14+ requesting dataSync from a service
             whose declared type doesn't include it throws at startForeground
             (crash-looped at launch when a download retry was pending). Merge the
             type in here. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
